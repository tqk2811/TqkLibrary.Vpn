# .NET self-contained publish of the interop harness (linux-x64 binaries) — rebuilt on demand, do not commit.
harness/publish/
harness/bin/
harness/obj/
# Published .NET client edge (self-contained linux-x64) copied into the live lab — rebuilt on demand, do not commit.
client-bin/
*.tar.gz
*.pcap
