# /etc/ppp/chap-secrets — xác thực CHAP / MS-CHAPv2 cho lab/l2tp-nonat.
# client    server    secret    IP addresses
# user 'vpn' pass 'vpn', mọi server ('*'), mọi IP ('*'). Khớp default vpn:vpn của demo.
vpn         *         vpn       *
